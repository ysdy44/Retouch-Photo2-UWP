<UserControl
    x:Class="Retouch_Photo2.Brushs.BrushTypeComboBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="200"
    >

    <UserControl.Resources>

        <!--Storyboard-->
        <Storyboard x:Name="EaseStoryboard">
            <ColorAnimation From="{ThemeResource SystemBaseHighColor}" To="{ThemeResource AppBorderBrushColor}" Storyboard.TargetProperty="Color" Storyboard.TargetName="ButtonBorderBrush" d:IsOptimized="True">
                <ColorAnimation.EasingFunction>
                    <BounceEase EasingMode="EaseOut"/>
                </ColorAnimation.EasingFunction>
            </ColorAnimation>
        </Storyboard>
        
        <!--Flyout-->
        <Flyout x:Name="Flyout" FlyoutPresenterStyle="{StaticResource AppFlyoutPresenterStyle}">
            <StackPanel Width="Auto" MinWidth="120" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                <Button x:Name="None" Style="{StaticResource AppIconSelectedButton}">
                    <Button.Resources>
                        <ResourceDictionary Source="ms-appx:///Retouch Photo2.Brushs/BrushTypes/BrushTypeIcons/NoneIcon.xaml"/>
                    </Button.Resources>
                    <Button.Tag>
                        <ContentControl Template="{StaticResource NoneIcon}"/>
                    </Button.Tag>
                </Button>
                
                <Rectangle Style="{StaticResource AppSeparatorRectangleVerticalStyle}"/>

                <Button x:Name="Color" Style="{StaticResource AppIconSelectedButton}">
                    <Button.Resources>
                        <ResourceDictionary Source="ms-appx:///Retouch Photo2.Brushs/BrushTypes/BrushTypeIcons/ColorIcon.xaml"/>
                    </Button.Resources>
                    <Button.Tag>
                        <ContentControl Template="{StaticResource ColorIcon}"/>
                    </Button.Tag>
                </Button>

                <Rectangle Style="{StaticResource AppSeparatorRectangleVerticalStyle}"/>
                
                <Button x:Name="LinearGradient" Style="{StaticResource AppIconSelectedButton}">
                    <Button.Resources>
                        <ResourceDictionary Source="ms-appx:///Retouch Photo2.Brushs/BrushTypes/BrushTypeIcons/LinearGradientIcon.xaml"/>
                    </Button.Resources>
                    <Button.Tag>
                        <ContentControl Template="{StaticResource LinearGradientIcon}"/>
                    </Button.Tag>
                </Button>
                <Button x:Name="RadialGradient" Style="{StaticResource AppIconSelectedButton}">
                    <Button.Resources>
                        <ResourceDictionary Source="ms-appx:///Retouch Photo2.Brushs/BrushTypes/BrushTypeIcons/RadialGradientIcon.xaml"/>
                    </Button.Resources>
                    <Button.Tag>
                        <ContentControl Template="{StaticResource RadialGradientIcon}"/>
                    </Button.Tag>
                </Button>
                <Button x:Name="EllipticalGradient" Style="{StaticResource AppIconSelectedButton}">
                    <Button.Resources>
                        <ResourceDictionary Source="ms-appx:///Retouch Photo2.Brushs/BrushTypes/BrushTypeIcons/EllipticalGradientIcon.xaml"/>
                    </Button.Resources>
                    <Button.Tag>
                        <ContentControl Template="{StaticResource EllipticalGradientIcon}"/>
                    </Button.Tag>
                </Button>
                
                <Rectangle Style="{StaticResource AppSeparatorRectangleVerticalStyle}"/>

                <Button x:Name="Image" Style="{StaticResource AppIconSelectedButton}">
                    <Button.Resources>
                        <ResourceDictionary Source="ms-appx:///Retouch Photo2.Brushs/BrushTypes/BrushTypeIcons/ImageIcon.xaml"/>
                    </Button.Resources>
                    <Button.Tag>
                        <ContentControl Template="{StaticResource ImageIcon}"/>
                    </Button.Tag>
                </Button>

            </StackPanel>
        </Flyout>
    </UserControl.Resources>


    <Button x:Name="Button" Style="{StaticResource AppIconRoundedButtonStyle}">

        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">

                <!--Normal-->
                <VisualState x:Name="Normal"/>

                <!--None-->
                <VisualState x:Name="NoneState">
                    <VisualState.Setters>
                        <Setter Target = "None.IsEnabled" Value="False"/>
                        <Setter Target = "Button.Content" Value="{x:Bind None.Content}"/>
                    </VisualState.Setters>
                </VisualState>

                <!--Color-->
                <VisualState x:Name="ColorState">
                    <VisualState.Setters>
                        <Setter Target = "Color.IsEnabled" Value="False"/>
                        <Setter Target = "Button.Content" Value="{x:Bind Color.Content}"/>
                    </VisualState.Setters>
                </VisualState>

                <!--LinearGradient-->
                <VisualState x:Name="LinearGradientState">
                    <VisualState.Setters>
                        <Setter Target = "LinearGradient.IsEnabled" Value="False"/>                        
                        <Setter Target = "Button.Content" Value="{x:Bind LinearGradient.Content}"/>
                    </VisualState.Setters>
                </VisualState>
                <!--RadialGradient-->
                <VisualState x:Name="RadialGradientState">
                    <VisualState.Setters>
                        <Setter Target = "RadialGradient.IsEnabled" Value="False"/>
                        <Setter Target = "Button.Content" Value="{x:Bind RadialGradient.Content}"/>
                    </VisualState.Setters>
                </VisualState>
                <!--EllipticalGradient-->
                <VisualState x:Name="EllipticalGradientState">
                    <VisualState.Setters>
                        <Setter Target = "EllipticalGradient.IsEnabled" Value="False"/>
                        <Setter Target = "Button.Content" Value="{x:Bind EllipticalGradient.Content}"/>
                    </VisualState.Setters>
                </VisualState>

                <!--Image-->
                <VisualState x:Name="ImageState">
                    <VisualState.Setters>
                        <Setter Target = "Image.IsEnabled" Value="False"/>
                        <Setter Target = "Button.Content" Value="{x:Bind Image.Content}"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        <Button.BorderBrush>
            <!--Storyboard-->
            <SolidColorBrush x:Name="ButtonBorderBrush" Color="{ThemeResource AppStrokeColor}"/>
        </Button.BorderBrush>
        
        <Button.Tag>
            <FontIcon FontSize="14" Glyph="&#xE099;" FontWeight="Bold" FontFamily="{ThemeResource SymbolThemeFontFamily}" />
        </Button.Tag>
    </Button>

</UserControl>