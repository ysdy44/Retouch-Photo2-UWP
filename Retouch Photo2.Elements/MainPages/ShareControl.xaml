<UserControl
    x:Class="Retouch_Photo2.Elements.MainPages.ShareControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo2.Elements.MainPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="800">


    <StackPanel x:Name="StackPanel" MinWidth="260">

        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>

                <!--Phone-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Vertical"/>
                    </VisualState.Setters>
                </VisualState>

                <!--Pad-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Horizontal"/>
                    </VisualState.Setters>
                </VisualState>

                <!--PC-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="900"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Horizontal"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <TextBlock Margin="8,2">
            <Run FontWeight="Bold">
                Share
            </Run>
            <LineBreak/>
            <Run FontSize="12" Foreground="{ThemeResource AppText}">
                Select project(s) to be shared.
            </Run>
        </TextBlock>

        <Border MinWidth="90" CornerRadius="2" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" BorderBrush="{ThemeResource AppStroke}" Background="{ThemeResource AppFill}">
            <ComboBox x:Name="ComboBox" SelectedIndex="0" BorderThickness="0" HorizontalAlignment="Stretch">
                <ComboBoxItem>
                    JPEG
                </ComboBoxItem>
                <ComboBoxItem BorderThickness="0,0,0,1" BorderBrush="{ThemeResource MenuFlyoutItemPointerOverBackgroundThemeBrush}">
                    PNG
                </ComboBoxItem>
                <ComboBoxItem>
                    BMP
                </ComboBoxItem>
                <ComboBoxItem>
                    GIF
                </ComboBoxItem>
                <ComboBoxItem>
                    TIFF
                </ComboBoxItem>
            </ComboBox>
        </Border>

        <StackPanel Orientation="Horizontal" Margin="8,0" HorizontalAlignment="Right">
            <local:MainPageButton x:Name="_OKButton" Glyph="&#xE10B;">
                <local:MainPageButton.Text>
                    OK
                </local:MainPageButton.Text>
            </local:MainPageButton>
            <local:MainPageButton x:Name="_CancelButton" Glyph="&#xE106;">
                <local:MainPageButton.Text>
                    Cancel
                </local:MainPageButton.Text>
            </local:MainPageButton>
        </StackPanel>

    </StackPanel>
</UserControl>