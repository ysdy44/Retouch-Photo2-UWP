<UserControl
    x:Class="Retouch_Photo2.Elements.MainPages.MainLayout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    
    xmlns:developerTools="using:Microsoft.Toolkit.Uwp.DeveloperTools"
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    
    xmlns:elements="using:Retouch_Photo2.Elements"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    d:DesignHeight="800"
    d:DesignWidth="800"
    mc:Ignorable="d"
    >

    
    <Grid x:Name="RootGrid">

        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">

                <!--Normal-->
                <VisualState x:Name="Normal"/>

                <!--Initial-->
                <VisualState x:Name="Initial">
                    <VisualState.Setters>

                        <!--Body-->
                        <Setter Target = "InitialBorder.Visibility" Value="Visible"/>
                        <Setter Target = "ScrollViewer.Visibility" Value="Collapsed"/>

                        <!--Foot-->
                        <Setter Target = "RadiusAnimaPanel.Visibility" Value="Collapsed"/>

                    </VisualState.Setters>
                </VisualState>

                <!--Main-->
                <VisualState x:Name="Main">
                    <VisualState.Setters>

                        <!--Foot-->
                        <Setter Target = "MainBorder.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

                <!--Dialog-->
                <VisualState x:Name="Dialog">
                    <VisualState.Setters>

                        <!--Head-->
                        <Setter Target = "HeadBorder.Visibility" Value="Collapsed"/>
                        
                        <!--Foot-->
                        <Setter Target = "RadiusAnimaPanel.Visibility" Value="Collapsed"/>

                    </VisualState.Setters>
                </VisualState>

                <!--Pictures-->
                <VisualState x:Name="Pictures">
                    <VisualState.Setters>

                        <!--Foot-->
                        <Setter Target = "PicturesBorder.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

                <!--Rename-->
                <VisualState x:Name="Rename">
                    <VisualState.Setters>

                        <!--Head-->
                        <Setter Target = "HeadBorder.Visibility" Value="Collapsed"/>

                        <!--Foot-->
                        <Setter Target = "RenameBorder.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>
                
                <!--Save-->
                <VisualState x:Name="Save">
                    <VisualState.Setters>

                        <!--Select-->
                        <Setter Target = "_SelectCheckBox.IsChecked" Value="true"/>
                        <Setter Target = "SelectGrid.Visibility" Value="Visible"/>
                        <Setter Target = "SelectCountRun.Text" Value=""/>

                        <!--Foot-->
                        <Setter Target = "SaveBorder.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

                <!--Share-->
                <VisualState x:Name="Share">
                    <VisualState.Setters>

                        <!--Select-->
                        <Setter Target = "_SelectCheckBox.IsChecked" Value="true"/>
                        <Setter Target = "SelectGrid.Visibility" Value="Visible"/>
                        <Setter Target = "SelectCountRun.Text" Value=""/>

                        <!--Foot-->
                        <Setter Target = "ShareBorder.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

                <!--Delete-->
                <VisualState x:Name="Delete">
                    <VisualState.Setters>

                        <!--Select-->
                        <Setter Target = "_SelectCheckBox.IsChecked" Value="true"/>
                        <Setter Target = "SelectGrid.Visibility" Value="Visible"/>
                        <Setter Target = "SelectCountRun.Text" Value=""/>

                        <!--Foot-->
                        <Setter Target = "DeleteBorder.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

                <!--Duplicate-->
                <VisualState x:Name="Duplicate">
                    <VisualState.Setters>

                        <!--Select-->
                        <Setter Target = "_SelectCheckBox.IsChecked" Value="true"/>
                        <Setter Target = "SelectGrid.Visibility" Value="Visible"/>
                        <Setter Target = "SelectCountRun.Text" Value=""/>

                        <!--Foot-->
                        <Setter Target = "DuplicateBorder.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

                <!--Move-->
                <VisualState x:Name="Move">
                    <VisualState.Setters>

                        <!--Select-->
                        <Setter Target = "SelectGrid.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>


        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" MinHeight="70"/>
        </Grid.RowDefinitions>

        <!--Developer-->
        <developerTools:AlignmentGrid Grid.RowSpan="3" Opacity="0.1" HorizontalStep="20" VerticalStep="20" LineBrush="Gray"/>


        <!--Head-->
        <Grid x:Name="HeadBorder" HorizontalAlignment="Stretch" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource AppStroke}" Background="{ThemeResource AppForeground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" MinWidth="50" MaxWidth="80"/>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="1*" MinWidth="50" MaxWidth="80"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="_RefreshButton" Grid.Column="0" Width="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="1" BorderBrush="Transparent" Background="Transparent">
                <Button.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="25" FromVerticalOffset="0"/>
                    </TransitionCollection>
                </Button.Transitions>
                <FontIcon FontSize="20" Glyph="&#xE117;" FontFamily="{ThemeResource SymbolThemeFontFamily}" />
            </Button>

            <TextBlock Grid.Column="1" HorizontalAlignment="Center" Style="{StaticResource SubheaderTextBlockStyle}">
                <TextBlock.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="50" FromVerticalOffset="0"/>
                    </TransitionCollection>
                </TextBlock.Transitions>
                        Retouh Photo2
            </TextBlock>

            <Button x:Name="_SettingButton" Grid.Column="2" Width="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="1" BorderBrush="Transparent" Background="Transparent">
                <Button.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="25" FromVerticalOffset="0"/>
                    </TransitionCollection>
                </Button.Transitions>
                <FontIcon FontSize="22" Glyph="&#xE115;" FontFamily="Segoe UI Symbol" />
            </Button>

        </Grid>

        <Grid x:Name="SelectGrid" Visibility="Collapsed" HorizontalAlignment="Stretch" Background="{ThemeResource AppBorderBrush}">

            <!--Visibility:Show-->
            <animations:Implicit.ShowAnimations>
                <animations:TranslationAnimation Duration="0:0:0.4" From="0,-50,0" To="0,0,0"/>
            </animations:Implicit.ShowAnimations>

            <!--Visibility:Hide-->
            <animations:Implicit.HideAnimations>
                <animations:TranslationAnimation Duration="0:0:0.4" From="0,0,0" To="0,-50,0"/>
            </animations:Implicit.HideAnimations>

            <CheckBox x:Name="_SelectCheckBox" Margin="12, 0" IsHitTestVisible="False" HorizontalAlignment="Left">
                <TextBlock>                        
                    Click to select items:                         
                    <Run x:Name="SelectCountRun">0</Run>
                </TextBlock>
            </CheckBox>

            <Button x:Name="_SelectAllButton" Padding="8,0" HorizontalAlignment="Right" VerticalAlignment="Stretch" BorderThickness="1" BorderBrush="Transparent" Background="Transparent">
                Select All
            </Button>
            
        </Grid>


        <!--Body-->
        <Border x:Name="InitialBorder" Grid.Row="1" Grid.RowSpan="2" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
        </Border>

        <ScrollViewer x:Name="ScrollViewer" Grid.Row="1" Grid.RowSpan="2" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <GridView x:Name="GridView" ReorderMode="Disabled" SelectionMode="None">
                <GridView.ItemContainerTransitions>
                    <TransitionCollection>
                        <AddDeleteThemeTransition />
                        <ContentThemeTransition />
                        <ReorderThemeTransition />
                        <EntranceThemeTransition IsStaggeringEnabled="False" />
                    </TransitionCollection>
                </GridView.ItemContainerTransitions>
            </GridView>
        </ScrollViewer>


        <!--Foot-->
        <elements:RadiusAnimaPanel x:Name="RadiusAnimaPanel" Grid.Row="2"
                                   ShadowOpacity="0.9"
                                   ShadowColor="{ThemeResource AppStrokeColor}"
                                   Background="{ThemeResource AppForeground}"
                                  >

            <!--Visibility:Show-->
            <animations:Implicit.ShowAnimations>
                <animations:TranslationAnimation Duration="0:0:0.4" From="0,100,0" To="0,0,0"/>
            </animations:Implicit.ShowAnimations>

            <!--Visibility:Hide-->
            <animations:Implicit.HideAnimations>
                <animations:TranslationAnimation Duration="0:0:0.4" From="0,0,0" To="0,100,0"/>
            </animations:Implicit.HideAnimations>

            <elements:RadiusAnimaPanel.CenterContent>
                <Grid>
                    <Border x:Name="MainBorder" Visibility="Collapsed"/>
                    <Border x:Name="PicturesBorder" Visibility="Collapsed"/>
                    <Border x:Name="RenameBorder" Visibility="Collapsed"/>
                    <Border x:Name="SaveBorder" Visibility="Collapsed"/>
                    <Border x:Name="ShareBorder" Visibility="Collapsed"/>
                    <Border x:Name="DeleteBorder" Visibility="Collapsed"/>
                    <Border x:Name="DuplicateBorder" Visibility="Collapsed"/>
                </Grid>
            </elements:RadiusAnimaPanel.CenterContent>

        </elements:RadiusAnimaPanel>
        
    </Grid>

</UserControl>