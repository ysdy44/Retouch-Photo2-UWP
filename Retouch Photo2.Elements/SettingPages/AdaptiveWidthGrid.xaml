<UserControl
    x:Class="Retouch_Photo2.Elements.AdaptiveWidthGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hsvColorPickers="using:HSVColorPickers"
    mc:Ignorable="d"
    d:DesignWidth="400"
    d:DesignHeight="50"
    >

    <UserControl.Resources>
        <!--Style-->
        <Style x:Key="RectangleStyle" TargetType="Rectangle">
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="StrokeThickness" Value="1"/>
         </Style>
    </UserControl.Resources>


    <Grid>

        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">

                <!--Normal-->
                <VisualState x:Name="Normal"/>

                <!--Disable-->
                <VisualState x:Name="Disable">
                    <VisualState.Setters >


                        <Setter Target = "PhoneIcon.Foreground" Value="Gray"/>
                        <Setter Target = "PadIcon.Foreground" Value="Gray"/>
                        <Setter Target = "PCIcon.Foreground" Value="Gray"/>

                        <!--
                        <Setter Target = "PhoneRectangle.Stroke" Value="Gray"/>
                        <Setter Target = "PadRectangle.Stroke" Value="Gray"/>
                        <Setter Target = "PCRectangle.Stroke" Value="Gray"/>
                        
                        <Setter Target = "PhoneSeparator.Fill" Value="Gray"/>
                        <Setter Target = "PadSeparator.Fill" Value="Gray"/>
                     

                        <Setter Target = "_PhoneNumberPicker.IsEnabled" Value="False"/>
                        <Setter Target = "_PhoneNumberPicker.IsEnabled" Value="False"/>
   -->
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="PhoneGridLength" Width="1*" MinWidth="50"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition x:Name="PadGridLength" Width="1*" MinWidth="50"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition x:Name="PCGridLength" Width="1*" MinWidth="50"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!--Top-->
        <Border Grid.Column="0" Style="{StaticResource AppRoundedBorderStyle}">
            <Grid>
                <Rectangle x:Name="PhoneRectangle" StrokeDashArray="1 2" Stroke="{ThemeResource SystemAccentColor}" Style="{StaticResource RectangleStyle}"/>
                <Border Padding="4,0" HorizontalAlignment="Center" Background="{ThemeResource AppForeground}">
                    <FontIcon x:Name="PhoneIcon" FontSize="22" Glyph="&#xE8EA;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                </Border>
            </Grid>
        </Border>      
        <Border Grid.Column="2" Style="{StaticResource AppRoundedBorderStyle}">
            <Grid>
                <Rectangle x:Name="PadRectangle" StrokeDashArray="1 2" Stroke="{ThemeResource SystemAccentColor}" Style="{StaticResource RectangleStyle}"/>
                <Border Padding="4,0" HorizontalAlignment="Center" Background="{ThemeResource AppForeground}">
                    <FontIcon x:Name="PadIcon" FontSize="22" Glyph="&#xE70A;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                </Border>
            </Grid>
        </Border>
        <Border Grid.Column="4" Style="{StaticResource AppRoundedBorderStyle}">
            <Grid>
                <Rectangle x:Name="PCRectangle" StrokeDashArray="1 2" Stroke="{ThemeResource SystemAccentColor}" Style="{StaticResource RectangleStyle}"/>
                <Border Padding="4,0" HorizontalAlignment="Center" Background="{ThemeResource AppForeground}">
                    <FontIcon x:Name="PCIcon" FontSize="22" Glyph="&#xF71E;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                </Border>
            </Grid>
        </Border>


        <!--Thumb-->
        <Thumb x:Name="PhoneThumb" Grid.Column="1" Background="{ThemeResource SystemAccentColor}">
            <Thumb.Template>
                <ControlTemplate>
                    <Border Background="Transparent">
                        <Rectangle Width="1" Height="Auto" Fill="{TemplateBinding Background}"/>
                    </Border>             
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>        
        <Thumb x:Name="PadThumb" Grid.Column="3" Background="{ThemeResource SystemAccentColor}">
            <Thumb.Template>
                <ControlTemplate>
                    <Border Background="Transparent">
                        <Rectangle Width="1" Height="Auto" Fill="{TemplateBinding Background}"/>
                    </Border>
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>
     

        <!--Bottom-->
        <TextBlock Grid.Column="0" Grid.Row="2" Text="Phone" HorizontalAlignment="Center" Style="{StaticResource AppTextBlock}"/>
        <TextBlock x:Name="PhoneTextBlock" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Center"/>
        <TextBlock Grid.Column="2" Grid.Row="2" Text="Pad" HorizontalAlignment="Center" Style="{StaticResource AppTextBlock}"/>
        <TextBlock x:Name="PadTextBlock" Grid.Column="3" Grid.Row="2" HorizontalAlignment="Center"/>
        <TextBlock Grid.Column="4" Grid.Row="2" Text="PC" HorizontalAlignment="Center" Style="{StaticResource AppTextBlock}"/>
        
        
    </Grid>
</UserControl>