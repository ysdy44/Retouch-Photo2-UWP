<UserControl
    x:Class="Retouch_Photo2.Elements.TouchbarButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">


    <UserControl.Resources>
        <!--Background-->
        <SolidColorBrush x:Name="AccentColor"  Color="{ThemeResource SystemAccentColor}"/>
        <SolidColorBrush x:Name="UnAccentColor" Color="Transparent"/>
        <!--Foreground-->
        <SolidColorBrush x:Name="CheckColor" Color="White"/>
        <SolidColorBrush x:Name="UnCheckColor" Color="{ThemeResource AppTextColor}"/>
    </UserControl.Resources>


    <Border x:Name="RootBorder"
        Width="Auto" 
        MinWidth="80" 
        Height="40"      
        CornerRadius="20" 
        BorderThickness="1" 
        BorderBrush="{ThemeResource AppBorderBrush}"  
        Background="{x:Bind BoolToBackgroundConverter(IsChecked),Mode=OneWay}"  
        >
        <TextBlock x:Name="TextBlock" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"                             
                   Foreground="{x:Bind BoolToForegroundConverter(IsChecked),Mode=OneWay}"
                   />
    </Border>
</UserControl>