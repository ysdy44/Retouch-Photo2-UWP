<UserControl 
    x:Class="Retouch_Photo2.Tools.ToolButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="50"
    d:DesignWidth="50"
    mc:Ignorable="d"
    >

    <ToolTipService.ToolTip>
        <ToolTip x:Name="_ToolTip" Placement="Right" Style="{StaticResource AppToolTipStyle}"
               Content="{x:Bind Title, Mode=OneWay}" />
    </ToolTipService.ToolTip>

    <Button x:Name="Button" Height="50" BorderThickness="0" Background="Transparent" VerticalAlignment="Stretch" Style="{StaticResource AppSelectedButtonStyle}"/>

</UserControl>