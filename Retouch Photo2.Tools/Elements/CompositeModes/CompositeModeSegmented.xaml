<UserControl
    x:Class="Retouch_Photo2.Tools.Elements.CompositeModeSegmented"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="40"
    d:DesignHeight="50"
    d:DesignWidth="200"
    >


    <Grid>

        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">

                <!--Normal-->
                <VisualState x:Name="Normal"/>

                <!--New-->
                <VisualState x:Name="NewState">
                    <VisualState.Setters>
                        <Setter Target = "New.IsEnabled" Value="False"/>
                    </VisualState.Setters>
                </VisualState>

                <!--Add-->
                <VisualState x:Name="AddState">
                    <VisualState.Setters>
                        <Setter Target = "Add.IsEnabled" Value="False"/>
                    </VisualState.Setters>
                </VisualState>

                <!--Subtract-->
                <VisualState x:Name="SubtractState">
                    <VisualState.Setters>
                        <Setter Target = "Subtract.IsEnabled" Value="False"/>
                    </VisualState.Setters>
                </VisualState>

                <!--Intersect
                <VisualState x:Name="IntersectState">
                    <VisualState.Setters>
                        <Setter Target = "Intersect.IsEnabled" Value="False"/>
                    </VisualState.Setters>
                </VisualState>
                -->

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!--New-->
        <Button x:Name="New" Grid.Column="0" Style="{StaticResource AppRoundedSelectedButtonLeftStyle}">
            <Button.Resources>
                <ResourceDictionary Source="ms-appx:///Retouch Photo2.Tools/Elements/CompositeModes/Icons/NewIcon.xaml"/>
            </Button.Resources>
            <ToolTipService.ToolTip>
                <ToolTip Placement="Top" IsOpen="{x:Bind IsOpen, Mode=OneWay}" Style="{StaticResource AppToolTipStyle}"/>
            </ToolTipService.ToolTip>
            <ContentControl Template="{StaticResource NewIcon}"/>
        </Button>

        <!--Add-->
        <Button x:Name="Add" Grid.Column="1" Style="{StaticResource AppRoundedSelectedButtonCenterStyle}">
            <Button.Resources>
                <ResourceDictionary Source="ms-appx:///Retouch Photo2.Tools/Elements/CompositeModes/Icons/AddIcon.xaml"/>
            </Button.Resources>
            <ToolTipService.ToolTip>
                <ToolTip Placement="Top" IsOpen="{x:Bind IsOpen, Mode=OneWay}" Style="{StaticResource AppToolTipStyle}"/>
            </ToolTipService.ToolTip>
            <ContentControl Template="{StaticResource AddIcon}"/>
        </Button>

        <!--Subtract-->
        <Button x:Name="Subtract" Grid.Column="2" Style="{StaticResource AppRoundedSelectedButtonRightStyle}">
            <Button.Resources>
                <ResourceDictionary Source="ms-appx:///Retouch Photo2.Tools/Elements/CompositeModes/Icons/SubtractIcon.xaml"/>
            </Button.Resources>
            <ToolTipService.ToolTip>
                <ToolTip Placement="Top" IsOpen="{x:Bind IsOpen, Mode=OneWay}" Style="{StaticResource AppToolTipStyle}"/>
            </ToolTipService.ToolTip>
            <ContentControl Template="{StaticResource SubtractIcon}"/>
        </Button>

        <!--Intersect     
        <Button x:Name="Intersect" Grid.Column="3" Style="{StaticResource AppRoundedSelectedButtonRightStyle}">
            <Button.Resources>
                <ResourceDictionary Source="ms-appx:///Retouch Photo2.Tools/Elements/CompositeModes/Icons/IntersectIcon.xaml"/>
            </Button.Resources>
            <ToolTipService.ToolTip>
                <ToolTip Placement="Top" IsOpen="{x:Bind IsOpen, Mode=OneWay}" Style="{StaticResource AppToolTipStyle}"/>
            </ToolTipService.ToolTip>
            <ContentControl Template="{StaticResource IntersectIcon}"/>
        </Button>
        -->

    </Grid>

</UserControl>