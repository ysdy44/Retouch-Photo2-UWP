<UserControl
    x:Name="UserControl"
    x:Class="Retouch_Photo2.Tools.TouchbarSlider"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo2.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hsvColorPickers="using:HSVColorPickers"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Center" 
    MinWidth="140"
    Height="36"   
    d:DesignHeight="50"
    d:DesignWidth="400"
    >

    <UserControl.Resources>
        <SolidColorBrush x:Key="ColorBorderBrush" Color="Black"/>
        <SolidColorBrush x:Key="ColorBackground" Color="#AAFFFFFF"/>
        <SolidColorBrush x:Key="ColorForeground" Color="White"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" MaxWidth="90"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>


        <hsvColorPickers:NumberPicker x:Name="NumberPicker" FlyoutPresenterStyle="{StaticResource AppFlyoutPresenterStyle}">
            <hsvColorPickers:NumberPicker.ButtonStyle>
                <Style TargetType="Button" BasedOn="{StaticResource AppNumberButtonStyle}">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="BorderBrush" Value="{StaticResource ColorBorderBrush}"/>
                    <Setter Property="Background" Value="{StaticResource ColorBackground}"/>
                    <Setter Property="Foreground" Value="{StaticResource ColorBorderBrush}"/>
                </Style>
            </hsvColorPickers:NumberPicker.ButtonStyle>
        </hsvColorPickers:NumberPicker>


        <Grid Grid.Column="2">

            <Border Height="32" Margin="1,0" Padding="0" CornerRadius="15" BorderThickness="3" BorderBrush="{StaticResource ColorBackground}"/>
            <Border Height="34" Margin="0" Padding="0" CornerRadius="16" BorderThickness="1" BorderBrush="{StaticResource ColorBorderBrush}"/>
            <Border Height="26" Margin="4,0" Padding="0" CornerRadius="13" BorderThickness="1" BorderBrush="{StaticResource ColorBorderBrush}"/>

            <Canvas x:Name="Canvas" Height="24" Margin="6,0" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <Ellipse x:Name="Ellipse" Width="24" Height="24" StrokeThickness="1" Stroke="{StaticResource ColorBorderBrush}" Fill="{StaticResource ColorForeground}"/>
            </Canvas>

            <Thumb x:Name="Thumb" ManipulationMode="TranslateX" Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Thumb.Style>
                    <Style TargetType="Thumb">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Thumb">
                                    <Border Background="Transparent"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Thumb.Style>
            </Thumb>

        </Grid>
    </Grid>
</UserControl>