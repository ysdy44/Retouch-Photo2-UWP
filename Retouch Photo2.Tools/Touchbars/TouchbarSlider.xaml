<hsvcolorpickers:TouchSliderBase
    x:Class="Retouch_Photo2.Tools.TouchbarSlider"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hsvcolorpickers="using:HSVColorPickers"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Center" 
    MinWidth="140"
    Height="36"
    d:DesignHeight="50"
    d:DesignWidth="100"
    >
        
    <Grid x:Name="_RootGrid" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="_LeftGridLength" Width="0"/>
            <ColumnDefinition x:Name="_CenterGridLength" Width="Auto"/>
            <ColumnDefinition x:Name="_RightGridLength" Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.ColumnSpan="3" Height="32" Margin="1,0" Padding="0" CornerRadius="15" BorderThickness="3" BorderBrush="{StaticResource ColorBackground}"/>
        <Border Grid.ColumnSpan="3" Height="34" Margin="0" Padding="0" CornerRadius="16" BorderThickness="1" BorderBrush="{StaticResource ColorBorderBrush}"/>
        <Border Grid.ColumnSpan="3" Height="26" Margin="4,0" Padding="0" CornerRadius="13" BorderThickness="1" BorderBrush="{StaticResource ColorBorderBrush}"/>

        <Ellipse Grid.Column="1" Margin="4,0"  Width="24" Height="24" StrokeThickness="1" Stroke="{StaticResource ColorBorderBrush}" Fill="{StaticResource ColorForeground}"/>

    </Grid>
</hsvcolorpickers:TouchSliderBase>   