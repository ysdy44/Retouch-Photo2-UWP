<Page
    x:Class="Retouch_Photo2.Adjustments.Pages.HighlightsAndShadowsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hsvColorPickers="using:HSVColorPickers"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="200"
    >

    <StackPanel >

        <!--Shadows-->
        <TextBlock x:Name="ShadowsTextBlock" Style="{StaticResource AppTextBlockStyle}"/>
        <hsvColorPickers:TouchSlider x:Name="ShadowsSlider" Minimum="-100" Maximum="100">
            <hsvColorPickers:TouchSlider.SliderBrush>
                <LinearGradientBrush  StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="Gray" Offset="1"/>
                </LinearGradientBrush>
            </hsvColorPickers:TouchSlider.SliderBrush>
        </hsvColorPickers:TouchSlider>

        <!--Highlights-->
        <TextBlock x:Name="HighlightsTextBlock" Style="{StaticResource AppTextBlockStyle}"/>
        <hsvColorPickers:TouchSlider x:Name="HighlightsSlider" Minimum="-100" Maximum="100">
            <hsvColorPickers:TouchSlider.SliderBrush>
                <LinearGradientBrush  StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Color="Gray" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </hsvColorPickers:TouchSlider.SliderBrush>
        </hsvColorPickers:TouchSlider>

        <!--Clarity-->
        <TextBlock x:Name="ClarityTextBlock" Style="{StaticResource AppTextBlockStyle}"/>
        <hsvColorPickers:TouchSlider x:Name="ClaritySlider" Minimum="-100" Maximum="100">
            <hsvColorPickers:TouchSlider.SliderBrush>
            <SolidColorBrush Color="{ThemeResource AppBorderBrushColor}"/>
            </hsvColorPickers:TouchSlider.SliderBrush>
        </hsvColorPickers:TouchSlider>

        <!--MaskBlurAmount-->
        <TextBlock x:Name="MaskBlurAmountTextBlock" Style="{StaticResource AppTextBlockStyle}"/>
        <hsvColorPickers:TouchSlider x:Name="MaskBlurAmountSlider" Minimum="0" Maximum="100">
            <hsvColorPickers:TouchSlider.SliderBrush>
                <SolidColorBrush Color="{ThemeResource AppBorderBrushColor}"/>
            </hsvColorPickers:TouchSlider.SliderBrush>
        </hsvColorPickers:TouchSlider>

        <ContentControl Height="8"/>
    </StackPanel>
    
</Page>