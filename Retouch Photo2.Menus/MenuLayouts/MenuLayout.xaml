<UserControl
    x:Class="Retouch_Photo2.Menus.MenuLayout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d" 
    >

    <!--Visibility:Show-->
    <animations:Implicit.ShowAnimations>
        <animations:OpacityAnimation Duration="0:0:0.2" From="0.0" To="1.0"/>
    </animations:Implicit.ShowAnimations>

    <!--Visibility:Hide-->
    <animations:Implicit.HideAnimations>
        <animations:OpacityAnimation Duration="0:0:0.2" From="1.0" To="0.0"/>
    </animations:Implicit.HideAnimations>

    
    <UserControl.Resources>
        
        <!--Storyboard-->
        <Storyboard x:Name="Storyboard" >
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Height)" Storyboard.TargetName="StoryboardRectangle" EnableDependentAnimation ="True">
                <SplineDoubleKeyFrame x:Name="Frame" KeyTime="0:0:0.2" Value="200" KeySpline="0.5,1,1,1"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <!--Style-->
        <Style x:Key="DropShadowPanelStyle" TargetType="controls:DropShadowPanel">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="OffsetY" Value="4"/>
            <Setter Property="BlurRadius" Value="20"/>
            <Setter Property="ShadowOpacity" Value="0.8"/>
            <Setter Property="Color" Value="{ThemeResource AppStrokeColor}"/>
        </Style>
        
    </UserControl.Resources>


    <controls:DropShadowPanel Style="{StaticResource DropShadowPanelStyle}">
        <Grid Width="222" BorderThickness="1" BorderBrush="{ThemeResource AppFill}" Background="{ThemeResource AppBackground}">

            <Rectangle x:Name="StoryboardRectangle"/>

            <Border x:Name="StoryboardBorder" VerticalAlignment="Top">
                <Border x:Name="ContentBorder">
                    <!--Content-->
                </Border>
            </Border>

        </Grid>
    </controls:DropShadowPanel>

</UserControl>