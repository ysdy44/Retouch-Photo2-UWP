<UserControl
    x:Class="Retouch_Photo.Elements.LabelControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="50">

    <UserControl.Resources>

        <!--Background-->
        <SolidColorBrush x:Name="AccentColor"  Color="{ThemeResource AppFillColor}"/>
        <SolidColorBrush x:Name="UnAccentColor"   Color="Transparent"/>

        <!--Foreground-->
        <SolidColorBrush x:Name="CheckColor"  Color="{ThemeResource SystemBaseHighColor}"/>
        <SolidColorBrush x:Name="UnCheckColor" Color="{ThemeResource SystemBaseMediumColor}"/>

    </UserControl.Resources>

    <ContentPresenter x:Name="ContentPresenter" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" BorderBrush="{ThemeResource AppStroke}"/>

</UserControl>
