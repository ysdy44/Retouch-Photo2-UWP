<effects:EffectPage
    x:Class="Retouch_Photo.Effects.Pages.EmbossPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:effects="using:Retouch_Photo.Effects"
    xmlns:element="using:Retouch_Photo.Elements"
    mc:Ignorable="d"
    >

    <StackPanel >
        <TextBlock Text="Amount" Style="{StaticResource AppTextBlock}"/>
        <Slider x:Name="AmountSlider" Minimum="0" Maximum="10" Style="{ThemeResource AppOpacitySliderStyle}"/>

        <TextBlock Text="Angle" Style="{StaticResource AppTextBlock}"/>
        <element:RadiansPicker x:Name="AnglePicker" HorizontalAlignment="Left" VerticalAlignment="Top"
                               BorderBrush="{ThemeResource AppBorderBrush}"
                               Background="{ThemeResource AppForeground}"
                               />
    </StackPanel>
    
</effects:EffectPage>