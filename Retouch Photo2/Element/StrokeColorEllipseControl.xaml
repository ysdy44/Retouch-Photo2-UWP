<UserControl
    x:Class="Retouch_Photo2.Element.StrokeColorEllipseControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hSVColorPickers="using:HSVColorPickers"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="50">

    <UserControl.Resources>
        <Flyout x:Name="ColorFlyout" FlyoutPresenterStyle="{StaticResource AppFlyoutPresenterStyle}">
            <hSVColorPickers:ColorPicker x:Name="ColorPicker" Width="222"/>
        </Flyout>
    </UserControl.Resources>

    <Border x:Name="ColorButton" Width="38" Height="38" Style="{StaticResource AppCircleBorderStyle}">
        <Border.Background>
            <ImageBrush ImageSource="ms-appx:///Icons\GrayAndWhite.jpg"/>
        </Border.Background>

        <Border>
            <Border.Background>
                <SolidColorBrush Color="{x:Bind ViewModel.StrokeColor,Mode=OneWay}"/>
            </Border.Background>
        </Border>
    </Border>

</UserControl>
