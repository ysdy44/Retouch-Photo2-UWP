<Page
    x:Class="Retouch_Photo2.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:developerTools="using:Microsoft.Toolkit.Uwp.DeveloperTools"
    xmlns:elements="using:Retouch_Photo2.Elements"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackground}"
    NavigationCacheMode="Required"    
    >


    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <SuppressNavigationTransitionInfo/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>


    <Page.Resources>
        <!--Extensions-->
        <elements:ApplicationViewTitleBarBackgroundExtension x:Key="AVTBBE" TitleBarColor="{ThemeResource AppForegroundColor}"/>
    </Page.Resources>
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!--Developer-->
        <developerTools:AlignmentGrid Grid.Row="1" Opacity="0.1" HorizontalStep="20" VerticalStep="20" LineBrush="Gray"/>

        
        <!--Body-->
        <ScrollViewer x:Name="ScrollViewer" Grid.RowSpan="2">
            <Border>
                <StackPanel Padding="20,0" MaxWidth="600">

                    <StackPanel.Resources>
                        <!--Style-->
                        <Style x:Name="KetButtonStyle" TargetType="ToggleButton">
                            <Setter Property="IsHitTestVisible" Value="False" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="Padding" Value="0,2" />
                            <Setter Property="Foreground" Value="{StaticResource AppText}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <StackPanel Orientation="Horizontal" Padding="{TemplateBinding Padding}">

                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="CommonStates">
                                                    <VisualState x:Name="Normal"/>

                                                    <VisualState x:Name="PointerOver"/>

                                                    <VisualState x:Name="Pressed"/>

                                                    <VisualState x:Name="Disabled"/>
                                                    <VisualState x:Name="Checked">
                                                        <Storyboard>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Border" Storyboard.TargetProperty="Background">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppForeground}" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundBaseHighBrush}" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                        </Storyboard>
                                                    </VisualState>
                                                    <VisualState x:Name="CheckedPointerOver">
                                                        <Storyboard>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Border" Storyboard.TargetProperty="Background">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppForeground}" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundBaseHighBrush}" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                        </Storyboard>
                                                    </VisualState>
                                                    <VisualState x:Name="CheckedPressed">
                                                        <Storyboard>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Border" Storyboard.TargetProperty="Background">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppForeground}" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlBackgroundBaseHighBrush}" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                        </Storyboard>
                                                    </VisualState>
                                                    <VisualState x:Name="CheckedDisabled"/>
                                                    <VisualState x:Name="Indeterminate"/>
                                                    <VisualState x:Name="IndeterminatePointerOver"/>
                                                    <VisualState x:Name="IndeterminatePressed"/>
                                                    <VisualState x:Name="IndeterminateDisabled"/>
                                                </VisualStateGroup>

                                            </VisualStateManager.VisualStateGroups>

                                            <ContentControl 
                            MinWidth="200"
                                Margin="4,0"                                                  
                                Content="{TemplateBinding Tag}"
                                Foreground="{TemplateBinding Foreground}"                                            
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"                                                   
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"                                                  
                                />
                                            <Border x:Name="Border" Width="60" HorizontalAlignment="Left" Background="{ThemeResource SystemControlBackgroundAccentBrush}" Style="{StaticResource AppRoundedBorderStyle}">
                                                <ContentPresenter x:Name="ContentPresenter" Content="{TemplateBinding Content}"                       
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"  
                                                  ContentTransitions="{TemplateBinding ContentTransitions}"                        
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"                       
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                                  Foreground="White"
                                                  />
                                            </Border>
                                        </StackPanel>

                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </StackPanel.Resources>
                    
                    <StackPanel.ChildrenTransitions>
                        <TransitionCollection>
                            <EntranceThemeTransition IsStaggeringEnabled="True"/>
                        </TransitionCollection>
                    </StackPanel.ChildrenTransitions>

                    <StackPanel.Children>


                        <ContentControl HorizontalAlignment="Center" ContentTemplate="{StaticResource AppListViewBaseHeader}"/>

                        
                        <ContentControl Height="12"/>


                        <!--Layout-->
                        <Border x:Name="LayoutBorder" Width="330" Height="210" Background="White" HorizontalAlignment="Left">
                            <!--Layout-->
                        </Border>
                        
                        
                        <!--Theme-->
                        <TextBlock Style="{StaticResource TitleTextBlockStyle}">
                            Theme
                        </TextBlock>
                        <Border MaxHeight="100" HorizontalAlignment="Left" Style="{StaticResource AppRoundedBorderStyle}">
                            <StackPanel Orientation="Horizontal">

                                <RadioButton x:Name="LightRadioButton" Tag="Light" GroupName="Theme" Style="{StaticResource AppTapRadioButtonStyle}">
                                    <FontIcon FontSize="20" Glyph="&#xE708;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                </RadioButton>
                                <RadioButton x:Name="DarkRadioButton" Tag="Dark" GroupName="Theme" Style="{StaticResource AppTapRadioButtonStyle}">
                                    <FontIcon FontSize="26" Glyph="&#xE706;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                </RadioButton>
                                <RadioButton x:Name="DefaultRadioButton" Tag="Use System" GroupName="Theme" Style="{StaticResource AppTapRadioButtonStyle}">
                                    <FontIcon FontSize="22" Glyph="&#xE7E8;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                </RadioButton>

                            </StackPanel>
                        </Border>

                        
                        <ContentControl Height="12"/>

                        
                        <!--Layout-->
                        <TextBlock Style="{StaticResource TitleTextBlockStyle}">
                            Layout
                        </TextBlock>
                        <Border MaxHeight="100" HorizontalAlignment="Left" Style="{StaticResource AppRoundedBorderStyle}">
                            <StackPanel Orientation="Horizontal">

                                <RadioButton x:Name="PhoneButton" Tag="Phone" GroupName="Layout" Style="{StaticResource AppTapRadioButtonStyle}">
                                    <FontIcon FontSize="22" Glyph="&#xE8EA;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                </RadioButton>
                                <RadioButton x:Name="PadButton" Tag="Pad" GroupName="Layout" Style="{StaticResource AppTapRadioButtonStyle}">
                                    <FontIcon FontSize="22" Glyph="&#xE70A;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                </RadioButton>
                                <RadioButton x:Name="PCButton" Tag="PC" GroupName="Layout" Style="{StaticResource AppTapRadioButtonStyle}">
                                    <FontIcon FontSize="22" Glyph="&#xF71E;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                </RadioButton>
                                <Rectangle Width="1" Fill="{ThemeResource AppBorderBrush}"/>
                                <RadioButton x:Name="AdaptiveButton" Tag="Adaptive" GroupName="Layout" Style="{StaticResource AppTapRadioButtonStyle}">
                                    <FontIcon FontSize="32" Glyph="&#xEBC6;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                </RadioButton>

                            </StackPanel>
                        </Border>
                        
                        
                        <ContentControl Height="12"/>


                        <!--Adaptive-->
                        <TextBlock x:Name="AdaptiveTextBlock" Style="{StaticResource TitleTextBlockStyle}">
                            Adaptive Width
                        </TextBlock>
                        <elements:AdaptiveWidthGrid x:Name="AdaptiveGrid"/>
                        <HyperlinkButton x:Name="AdaptiveResetButton" Content="Reset adaptive width"/>

                        
                        <ContentControl Height="12"/>


                        <!--Key-->
                        <TextBlock Style="{StaticResource TitleTextBlockStyle}">
                            Shortcuts
                        </TextBlock>
                        <ToggleButton Tag="IsCenter" Content="Ctrl" IsChecked="{x:Bind KeyboardViewModel.IsCenter, Mode=OneWay}" Style="{StaticResource KetButtonStyle}"/>
                        <ToggleButton Tag="IsRatio" Content="Shift" IsChecked="{x:Bind KeyboardViewModel.IsRatio, Mode=OneWay}" Style="{StaticResource KetButtonStyle}"/>
                        <ToggleButton Tag="IsSquare" Content="Shift" IsChecked="{x:Bind KeyboardViewModel.IsSquare, Mode=OneWay}" Style="{StaticResource KetButtonStyle}"/>
                        <ToggleButton Tag="IsStepFrequency" Content="Space" IsChecked="{x:Bind KeyboardViewModel.IsStepFrequency, Mode=OneWay}" Style="{StaticResource KetButtonStyle}"/>

                        <ToggleButton Tag="FullScreen" Content="Esc" IsChecked="{x:Bind KeyboardViewModel.IsFullScreen, Mode=OneWay}" Style="{StaticResource KetButtonStyle}"/>


                        <!--Local-->
                        <TextBlock Style="{StaticResource TitleTextBlockStyle}">
                            Local Folder
                        </TextBlock>
                        <Button x:Name="LocalFolderButton" HorizontalAlignment="Left" Style="{ThemeResource AppTagLineCircleButton}">
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE17C;" FontSize="24" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                <ContentControl Width="8"/>
                                <TextBlock VerticalAlignment="Center">
                                    Open
                                </TextBlock>
                            </StackPanel>
                        </Button>


                        <ContentControl HorizontalAlignment="Center" ContentTemplate="{StaticResource AppListViewBaseFooter}"/>


                    </StackPanel.Children>

                </StackPanel>
            </Border>
        </ScrollViewer>


        <!--Head-->
        <Grid HorizontalAlignment="Stretch" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource AppStroke}" Background="{ThemeResource AppForeground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" MinWidth="50" MaxWidth="80"/>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="1*" MinWidth="50" MaxWidth="80"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="BackButton" Width="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="1" BorderBrush="Transparent" Background="Transparent">
                <Button.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="25" FromVerticalOffset="0"/>
                    </TransitionCollection>
                </Button.Transitions>
                <FontIcon Glyph="&#xE0D5;" FontSize="20" FontFamily="{ThemeResource SymbolThemeFontFamily}" />
            </Button>

            <TextBlock Grid.Column="1" HorizontalAlignment="Center" Style="{StaticResource SubheaderTextBlockStyle}">
                <TextBlock.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="50" FromVerticalOffset="0"/>
                    </TransitionCollection>
                </TextBlock.Transitions>
                Setting
            </TextBlock>

        </Grid>

    </Grid>
</Page>