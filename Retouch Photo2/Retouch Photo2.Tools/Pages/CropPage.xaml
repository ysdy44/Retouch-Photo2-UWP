<Page
    x:Class="Retouch_Photo2.Tools.Pages.CropPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:elements="using:Retouch_Photo2.Elements"
    xmlns:toolsElements="using:Retouch_Photo2.Tools.Elements"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="400"
    >


    <StackPanel Orientation="Horizontal">

        <!--Reset-->
        <TextBlock Style="{StaticResource AppTextBlock}">
            Reset
        </TextBlock>
        <Button x:Name="ResetButton" Style="{StaticResource AppCircleButtonStyle}"
                 IsEnabled="{x:Bind SelectionViewModel.IsCrop, Mode=OneWay}">
            <FontIcon Glyph="&#xF617;" FontSize="24" FontFamily="Segoe MDL2 Assets" Foreground="{ThemeResource AppText}"/>
        </Button>

        <ContentControl Width="4"/>

        <!--Fit-->
        <TextBlock Style="{StaticResource AppTextBlock}">
            Fit
        </TextBlock>
        <Button x:Name="FitButton" Style="{StaticResource AppCircleButtonStyle}"
                 IsEnabled="{x:Bind SelectionViewModel.IsCrop, Mode=OneWay}">
            <FontIcon Glyph="&#xE9A6;" FontSize="22" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource AppText}"/>
        </Button>

        <ContentControl Width="4"/>

        <toolsElements:MoreCreateButton/>

    </StackPanel>
</Page>