<UserControl
    x:Class="Retouch_Photo2.Controls.SelectionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:selections="using:Retouch_Photo2.Selections"
    xmlns:selectionsEdi2="using:Retouch_Photo2.Selections.Edit2Controls"
    xmlns:selectionsEdi="using:Retouch_Photo2.Selections.EditControls"
    xmlns:selectionsSel2="using:Retouch_Photo2.Selections.Select2Controls"
    xmlns:selectionsSel="using:Retouch_Photo2.Selections.SelectControls"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="300"
    >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!--Edit-->
        <StackPanel Grid.Column="0">

            <selections:Button x:Name="CutButton" Label="Cut">
                <selections:Button.EnabledIcon>
                    <selectionsEdi:CutEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsEdi:CutDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <selections:Button x:Name="CopyButton" Label="Copy">
                <selections:Button.EnabledIcon>
                    <selectionsEdi:CopyEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsEdi:CopyDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <selections:Button x:Name="PasteButton" Label="Paste">
                <selections:Button.EnabledIcon>
                    <selectionsEdi:PasteEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsEdi:PasteDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <selections:Button x:Name="RemoveButton" Label="Clear">
                <selections:Button.EnabledIcon>
                    <selectionsEdi:ClearEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsEdi:ClearDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <Rectangle Margin="12,0" Height="1" StrokeDashArray="2 2" Stroke="{ThemeResource AppBorderBrush}" />

            <selections:Button x:Name="ExtractButton" Label="Extract">
                <selections:Button.EnabledIcon>
                    <selectionsEdi2:ExtractEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsEdi2:ExtractDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <selections:Button x:Name="MergeButton" Label="Merge">
                <selections:Button.EnabledIcon>
                    <selectionsEdi2:MergeEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsEdi2:MergeDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <Ellipse x:Name="AA" Width="22" Height="22" Fill="Red"/>
            <Ellipse x:Name="BB" Width="22" Height="22" Fill="Green"/>
            <TextBlock x:Name="CC"/>
        </StackPanel>

        <Rectangle Grid.Column="1" Width="1" StrokeDashArray="2 2" Stroke="{ThemeResource AppBorderBrush}" />

        <!--Select-->
        <StackPanel Grid.Column="2">

            <selections:Button x:Name="AllButton" Label="All">
                <selections:Button.EnabledIcon>
                    <selectionsSel:AllEnabledControl VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsSel:AllDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <selections:Button x:Name="DeselectButton" Label="Deselect">
                <selections:Button.EnabledIcon>
                    <selectionsSel:DeselectEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsSel:DeselectDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <selections:Button x:Name="PixelButton" Label="Pixel">
                <selections:Button.EnabledIcon>
                    <selectionsSel:PixelEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsSel:PixelDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <selections:Button x:Name="InvertButton" Label="Invert">
                <selections:Button.EnabledIcon>
                    <selectionsSel:InvertEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsSel:InvertDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <Rectangle Margin="12,0" Height="1" StrokeDashArray="2 2" Stroke="{ThemeResource AppBorderBrush}" />

            <selections:Button x:Name="FeatherButton" Label="Feather">
                <selections:Button.EnabledIcon>
                    <selectionsSel2:FeatherEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsSel2:FeatherDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

            <selections:Button x:Name="TransformButton" Label="Transform">
                <selections:Button.EnabledIcon>
                    <selectionsSel2:TransformEnabledControl/>
                </selections:Button.EnabledIcon>
                <selections:Button.DisabledIcon>
                    <selectionsSel2:TransformDisabledControl/>
                </selections:Button.DisabledIcon>
            </selections:Button>

        </StackPanel>

    </Grid>

</UserControl>
