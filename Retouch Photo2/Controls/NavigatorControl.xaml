<UserControl
    x:Class="Retouch_Photo2.Controls.NavigatorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo2.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:element="using:Retouch_Photo2.Element"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="200">

    <StackPanel Background="{ThemeResource AppBackground}">


        <TextBlock Visibility="Collapsed"  Text="View"  VerticalAlignment="Center"/>
        <Border Visibility="Collapsed" MinHeight="122" Background="{ThemeResource AppForeground}">
            <xaml:CanvasControl x:Name="CanvasControl"/>
        </Border>

        <TextBlock Text="Marquee Mode:"  Style="{StaticResource AppTextBlock}"/>
        <element:MarqueeModeControl Width="Auto" Margin="2,0" Mode="{x:Bind ViewModel.MarqueeMode,Mode=TwoWay}" />

        <TextBlock Text="Scale:"  Style="{StaticResource AppTextBlock}"/>
        <Border Margin="2,0" Style="{StaticResource AppRoundedBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions >
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="FiftyPercent" Grid.Column="0" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Style="{ThemeResource DateTimePickerFlyoutButtonStyle}" Background="{ThemeResource AppForeground}">
                    <TextBlock Text="50%" FontSize="13"/>
                </Button>
                <Button x:Name="HundredPercent" Grid.Column="1" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Style="{ThemeResource DateTimePickerFlyoutButtonStyle}" Background="{ThemeResource AppForeground}">
                    <TextBlock Text="100%" FontSize="13"/>
                </Button>
                <Button x:Name="TwoHundredPercent" Grid.Column="2" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Style="{ThemeResource DateTimePickerFlyoutButtonStyle}" Background="{ThemeResource AppForeground}">
                    <TextBlock Text="200%" FontSize="13"/>
                </Button>
                <Button x:Name="AutoPercent" Grid.Column="3" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Style="{ThemeResource DateTimePickerFlyoutButtonStyle}" Background="{ThemeResource AppForeground}">
                    <TextBlock Text="Auto" FontSize="13"/>
                </Button>
            </Grid>
        </Border>        
                
        <ContentControl Height="8"/>

        <Grid Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="36"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xEC46;" FontSize="24" FontFamily="Segoe MDL2 Assets" Foreground="{ThemeResource SystemControlBackgroundBaseHighBrush}"/>
            <TextBlock Grid.Column="1" Text="Dark Theme" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlBackgroundBaseHighBrush}"/>
            <ToggleSwitch x:Name="ThemeSwitch" Grid.Column="2"/>
        </Grid>

        <Rectangle Height="1" Fill="{ThemeResource AppBorderBrush}"/>
        
        <Grid Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="36"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xE80A;" FontSize="20" FontFamily="Segoe MDL2 Assets" Foreground="{ThemeResource SystemControlBackgroundBaseHighBrush}"/>
            <TextBlock Grid.Column="1" Text="Show Rulers"  VerticalAlignment="Center" Foreground="{ThemeResource SystemControlBackgroundBaseHighBrush}"/>
            <ToggleSwitch x:Name="RulerSwitch" Grid.Column="2" />
        </Grid>


    </StackPanel>
</UserControl>
