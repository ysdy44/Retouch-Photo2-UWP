<UserControl
    x:Class="Retouch_Photo2.Controls.LayersControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="200"
    >

    <UserControl.Resources>
        <!--Flyout-->
        <Flyout x:Name="AddImageFlyout" Placement="Left" FlyoutPresenterStyle="{StaticResource AppFlyoutPresenterStyle}">
            <StackPanel>
                <Button x:Name="_PhotoButton" Height="50" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" HorizontalAlignment="Stretch">
                    <TextBlock Foreground="{ThemeResource AppText}">
                        New Image From Pictures
                    </TextBlock>
                </Button>
                <Button x:Name="_DestopButton" Height="50" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" HorizontalAlignment="Stretch">
                    <TextBlock Foreground="{ThemeResource AppText}">
                        New Image From Destop
                    </TextBlock>
                </Button>
                <Button x:Name="_PadButton" Height="50" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" HorizontalAlignment="Stretch">
                    <TextBlock Foreground="{ThemeResource AppText}">
                        Width
                    </TextBlock>
                </Button>
            </StackPanel>
        </Flyout>
    </UserControl.Resources>

    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Button x:Name="AddButton" Height="42" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="&#xE109;" FontFamily="{ThemeResource SymbolThemeFontFamily}" BorderThickness="0" BorderBrush="Transparent" Background="Transparent"/>
        <Slider x:Name="ControlHeightSlider" Margin="4,0" Grid.Row="3" Minimum="40" Maximum="80" StepFrequency="10" VerticalAlignment="Center"/>

        <Canvas Grid.Row="1">
            <Border x:Name="IndicatorBorder" MinWidth="50" Height="40"/>
        </Canvas>

        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Hidden"
                      >
            <ItemsControl x:Name="ItemsControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <!--Layers-->
            </ItemsControl>
        </ScrollViewer>
        
    </Grid>
</UserControl>