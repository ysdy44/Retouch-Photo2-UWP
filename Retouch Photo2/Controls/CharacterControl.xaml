<UserControl
    x:Class="Retouch_Photo2.Controls.CharacterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo2.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:menus="using:Retouch_Photo2.Menus"
    xmlns:elements="using:Retouch_Photo2.Elements"
    xmlns:hsvcolorpickers="using:HSVColorPickers"
    mc:Ignorable="d"
    d:DesignWidth="200"
    d:DesignHeight="400"
    >

    <Grid>
        
        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">

                <!--Normal-->
                <VisualState x:Name="Normal"/>

                <!--SelectFontFamily-->
                <VisualState x:Name="SelectFontFamily">
                    <VisualState.Setters>

                        <Setter Target = "_MenuTitle.IsSecondPage" Value="true"/>

                        <Setter Target = "MainStackPanel.Visibility" Value="Collapsed"/>
                        <Setter Target = "FontFamilyListView.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

                <!--SelectFontSize-->
                <VisualState x:Name="SelectFontSize">
                    <VisualState.Setters>

                        <Setter Target = "_MenuTitle.IsSecondPage" Value="true"/>

                        <Setter Target = "MainStackPanel.Visibility" Value="Collapsed"/>
                        <Setter Target = "FontSizeListView.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

                <!--SelectFontWeight-->
                <VisualState x:Name="SelectFontWeight">
                    <VisualState.Setters>

                        <Setter Target = "_MenuTitle.IsSecondPage" Value="true"/>

                        <Setter Target = "MainStackPanel.Visibility" Value="Collapsed"/>
                        <Setter Target = "FontWeightListView.Visibility" Value="Visible"/>

                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*" MinHeight="165"/>
        </Grid.RowDefinitions>

        <!--Head-->
        <menus:MenuTitle x:Name="_MenuTitle">
            <menus:MenuTitle.Text>
                Character
            </menus:MenuTitle.Text>
        </menus:MenuTitle>

        <!--FontFamily-->
        <ListView x:Name="FontFamilyListView" Grid.Row="1" Visibility="Collapsed" MaxHeight="300" IsItemClickEnabled="True" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock FontFamily="{x:Bind}" Text="{x:Bind}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--FontSize-->
        <ListView x:Name="FontSizeListView" Grid.Row="1" Visibility="Collapsed" MaxHeight="300" IsItemClickEnabled="True" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:Int32">
                    <TextBlock>
                        <Run Text="{x:Bind}"></Run>
                        <Run> px</Run>
                    </TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--FontWeight-->
        <ListView x:Name="FontWeightListView" Grid.Row="1" Visibility="Collapsed" MaxHeight="300" IsItemClickEnabled="True" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:FontWeightName">
                    <TextBlock Text="{x:Bind Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--Main-->
        <StackPanel x:Name="MainStackPanel" Grid.Row="1">

            <StackPanel.Resources>
                <Style x:Key="TransparentButton" TargetType="Button">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource AppText}"/>
                </Style>
            </StackPanel.Resources>

            
            <!--Horizontal Alignment-->
            <TextBlock Style="{StaticResource AppTextBlock}">
                Horizontal Alignment
            </TextBlock>            
            <Border Margin="2,0" Style="{StaticResource AppRoundedBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="HorizontalButton0" Grid.Column="0" Style="{StaticResource TransparentButton}">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8E4;" FontSize="18" />
                    </Button>
                    <Button x:Name="HorizontalButton1" Grid.Column="1" Style="{StaticResource TransparentButton}">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8E3;" FontSize="18" />
                    </Button>
                    <Button x:Name="HorizontalButton2" Grid.Column="2" Style="{StaticResource TransparentButton}">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8E2;" FontSize="18" />
                    </Button>
                    <Button x:Name="HorizontalButton3" Grid.Column="60" Style="{StaticResource TransparentButton}">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE700;" FontSize="18" />
                    </Button>
                    
                </Grid>
            </Border>


            <!--TextFontStyle-->
            <TextBlock Style="{StaticResource AppTextBlock}">
                Font Style
            </TextBlock>
            <Grid Margin="2,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource AppRoundedBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="FontWeightToggleButton" Grid.Column="0" Style="{StaticResource TransparentButton}">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8DD;" FontSize="18" />
                        </Button>
                        <Button x:Name="FontStyleToggleButton" Grid.Column="1" Style="{StaticResource TransparentButton}">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE199;" FontSize="18" />
                        </Button>
                        <Button x:Name="UnderLineToggleButton" IsEnabled="False" Grid.Column="2" Style="{StaticResource TransparentButton}">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE19A;" FontSize="18" />
                        </Button>
                    </Grid>
                </Border>


                <elements:IconRoundedBorderButton x:Name="FontWeightButton" Grid.Column="1" Margin="2,0">
                    <elements:IconRoundedBorderButton.Icon>
                        <FontIcon FontSize="14" Glyph="&#xE09B;" FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                    </elements:IconRoundedBorderButton.Icon>
                </elements:IconRoundedBorderButton>
                
            </Grid>
            
            
            <!--FontFamily-->
            <TextBlock Style="{StaticResource AppTextBlock}">
                FontFamily
            </TextBlock>
            <elements:IconRoundedBorderButton x:Name="FontFamilyButton" Margin="2,0" 
                     Text="{x:Bind SelectionViewModel.TextFontFamily, Mode=OneWay}">
                <elements:IconRoundedBorderButton.Icon>
                    <FontIcon FontSize="14" Glyph="&#xE09B;" FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                </elements:IconRoundedBorderButton.Icon>
            </elements:IconRoundedBorderButton>
            
            
            <!--FontSize-->
            <TextBlock Style="{StaticResource AppTextBlock}">
                FontSize
            </TextBlock>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <Border Style="{ThemeResource AppRoundedBorderStyle}">
                    <hsvcolorpickers:NumberPicker x:Name="FontSizePicker" Minimum="4" Maximum="1000"
                             Value="{x:Bind SelectionViewModel.TextFontSize, Mode=OneWay}"
                             Width="Auto" MaxWidth="200" HorizontalAlignment="Stretch"/>
                </Border>

                <elements:IconRoundedBorderButton x:Name="FontSizeButton" Grid.Column="1" Margin="2,0">
                    <elements:IconRoundedBorderButton.Icon>
                        <FontIcon FontSize="14" Glyph="&#xE09B;" FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                    </elements:IconRoundedBorderButton.Icon>
                </elements:IconRoundedBorderButton>

            </Grid>

            <ContentControl Height="2"/>

        </StackPanel>

    </Grid>

</UserControl>