<ContentDialog
    x:Class="Retouch_Photo2.Pages.MainPages.AddDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hSVColorPickers="using:HSVColorPickers"
    mc:Ignorable="d"
    BorderBrush="{ThemeResource AppStroke}"               
    Background="{ThemeResource AppFill}"                                
    PrimaryButtonText="OK"
    SecondaryButtonText="Cancel"
    >
    

    <ContentDialog.Title>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="ExpanedButton" VerticalAlignment="Center" Style="{ThemeResource TextBlockButtonStyle}">
                <FontIcon Glyph="&#xE96E;" FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
            </Button>
            <TextBlock  Text="Add New Project" VerticalAlignment="Center"/>
        </StackPanel>
    </ContentDialog.Title>

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" Text="Width" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="3" Text="Height" VerticalAlignment="Center"/>

        <Border Grid.Column="1" Grid.Row="1" Style="{StaticResource AppRoundedBorderStyle}">
            <hSVColorPickers:NumberPicker x:Name="WidthNumberPicker" Width="Auto"/>
        </Border>
        <Border Grid.Column="1" Grid.Row="3" Style="{StaticResource AppRoundedBorderStyle}">
            <hSVColorPickers:NumberPicker x:Name="HeighNumberPicker" Width="Auto"/>
        </Border>

    </Grid>
</ContentDialog>
