<UserControl
    x:Class="Retouch_Photo2.Pages.MainPages.AppbarControls.DeleteControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:elementAppbarControls="using:Retouch_Photo2.Pages.MainPages.AppbarControls"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="800">


    <StackPanel x:Name="StackPanel" MinWidth="260">

        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>

                <!--Phone-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Vertical"/>
                    </VisualState.Setters>
                </VisualState>

                <!--Pad-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Horizontal"/>
                    </VisualState.Setters>
                </VisualState>

                <!--PC-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="900"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Horizontal"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <StackPanel Margin="8" VerticalAlignment="Center">
            <TextBlock Text="Delete Projects" FontWeight="Bold"/>
            <TextBlock Text="Select project(s) to be deleted." FontSize="12" Style="{ThemeResource AppTextBlock}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <AppBarSeparator/>
            <elementAppbarControls:AppBarControlButton x:Name="OKButton" Glyph="&#xE10B;"  Text="OK" />
            <elementAppbarControls:AppBarControlButton x:Name="CancelButton" Glyph="&#xE106;"  Text="Cancel"/>
        </StackPanel>

    </StackPanel>

</UserControl>
