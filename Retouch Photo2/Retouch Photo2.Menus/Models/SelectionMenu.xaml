<UserControl
    x:Class="Retouch_Photo2.Menus.Models.SelectionMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:menus="using:Retouch_Photo2.Menus"
    xmlns:selections="using:Retouch_Photo2.Selections"
    mc:Ignorable="d"
    d:DesignWidth="200"
    d:DesignHeight="400"
    >


    <menus:MenuExpander x:Name="_Expander">

        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">

                <!--Normal-->
                <VisualState x:Name="Normal"/>

                <!--Disable-->
                <VisualState x:Name="Disable">
                    <VisualState.Setters>

                        <Setter Target = "CutButton.IsEnabled" Value="false"/>
                        <Setter Target = "CopyButton.IsEnabled" Value="false"/>
                        <Setter Target = "ClearButton.IsEnabled" Value="false"/>

                        <Setter Target = "InvertButton.IsEnabled" Value="false"/>

                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        
        <menus:MenuExpander.MainPage>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!--Edit-->
                <StackPanel Grid.Column="0">

                    <Button x:Name="CutButton" Style="{StaticResource AppIconButton}"/>
                    <Button x:Name="CopyButton" Style="{StaticResource AppIconButton}"/>
                    <Button x:Name="PasteButton" IsEnabled="False" Style="{StaticResource AppIconButton}"/>
                    <Button x:Name="ClearButton" Style="{StaticResource AppIconButton}"/>

                    <Rectangle Style="{ThemeResource AppSeparatorRectangle}" />

                    <Button x:Name="ExtractButton" IsEnabled="False" Style="{StaticResource AppIconButton}"/>
                    <Button x:Name="MergeButton" IsEnabled="False" Style="{StaticResource AppIconButton}"/>

                </StackPanel>

                <Rectangle Grid.Column="1" Width="1" StrokeDashArray="2 2" Stroke="{ThemeResource AppBorderBrush}" />

                <!--Select-->
                <StackPanel Grid.Column="2">

                    <Button x:Name="AllButton" IsEnabled="True" Style="{StaticResource AppIconButton}"/>
                    <Button x:Name="DeselectButton" IsEnabled="True" Style="{StaticResource AppIconButton}"/>
                    <Button x:Name="PixelButton" IsEnabled="False" Style="{StaticResource AppIconButton}"/>
                    <Button x:Name="InvertButton" Style="{StaticResource AppIconButton}"/>

                    <Rectangle Margin="12,0" Height="1" StrokeDashArray="2 2" Stroke="{ThemeResource AppBorderBrush}" />

                    <Button x:Name="FeatherButton" IsEnabled="False" Style="{StaticResource AppIconButton}"/>
                    <Button x:Name="TransformButton" IsEnabled="False" Style="{StaticResource AppIconButton}"/>

                </StackPanel>

            </Grid>
        </menus:MenuExpander.MainPage>
    </menus:MenuExpander>

</UserControl>