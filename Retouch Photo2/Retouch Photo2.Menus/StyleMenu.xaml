<UserControl
    x:Class="Retouch_Photo2.Menus.StyleMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:styles="using:Retouch_Photo2.Styles"
    mc:Ignorable="d"
    d:DesignWidth="200"
    d:DesignHeight="400"
    >


    <GridView x:Name="GridView" SelectionMode="None" IsItemClickEnabled="True">


        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">

                <!--Normal-->
                <VisualState x:Name="Normal"/>

                <!--Writable-->
                <VisualState x:Name="Writable">
                    <VisualState.Setters>
                        <Setter Target = "GridView.SelectionMode" Value="Multiple"/>
                        <Setter Target = "GridView.IsItemClickEnabled" Value="False"/>
                        <Setter Target = "AddButton.Visibility" Value="Collapsed"/>
                        <Setter Target = "WritableButton.Visibility" Value="Collapsed"/>
                        <Setter Target = "WritableOKButton.Visibility" Value="Visible"/>
                        <Setter Target = "WritableCancelButton.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>


        <GridView.ItemContainerStyle>
            <Style TargetType="GridViewItem">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Width" Value="74"/>
                <Setter Property="Height" Value="74"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </GridView.ItemContainerStyle>

        <GridView.GroupStyle>
            <GroupStyle>
                <GroupStyle.HeaderTemplate>
                    <DataTemplate x:DataType="styles:StyleCategory">
                        <TextBlock Text="{x:Bind Name}" Style="{StaticResource AppTabTextBlockStyle}"/>
                    </DataTemplate>
                </GroupStyle.HeaderTemplate>
            </GroupStyle>
        </GridView.GroupStyle>

        <GridView.ItemTemplate>
            <DataTemplate x:DataType="styles:Style">
                <styles:StyleShowControl Style2="{x:Bind}" HorizontalAlignment="Center">
                    <ToolTipService.ToolTip>
                        <ToolTip Content="{x:Bind Name}"/>
                    </ToolTipService.ToolTip>
                </styles:StyleShowControl>
            </DataTemplate>
        </GridView.ItemTemplate>

        <GridView.Header>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Padding="4,0">
                <Button x:Name="AddButton" Background="{ThemeResource AppForeground}" Style="{StaticResource AppRoundedButtonStyle}">
                    <SymbolIcon Symbol="Add"/>
                </Button >
                <Button x:Name="WritableButton" Background="{ThemeResource AppForeground}" Style="{StaticResource AppRoundedButtonStyle}">
                    <SymbolIcon Symbol="ShowBcc"/>
                </Button >
                <Button x:Name="WritableOKButton" Visibility="Collapsed" Style="{StaticResource AppAccentOutlineButton}">
                    <SymbolIcon Symbol="Accept"/>
                </Button>
                <Button x:Name="WritableCancelButton" Margin="4,0" Visibility="Collapsed" Style="{StaticResource AppOutlineButton}">
                    <SymbolIcon Symbol="Cancel"/>
                </Button>
            </StackPanel>
        </GridView.Header>

    </GridView>

</UserControl>