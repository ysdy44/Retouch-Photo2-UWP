<UserControl
    x:Class="Retouch_Photo2.ToolsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="50"
    >

    <UserControl.Resources>
        <!--Style-->
        <Style x:Name="SeparatorRectangle" TargetType="Rectangle" BasedOn="{StaticResource AppSeparatorRectangleVerticalStyle}"/>

        <Style x:Name="ToolTipStyle" TargetType="ToolTip" BasedOn="{StaticResource AppToolTipStyle}"/>

        <Style x:Name="IconSelectedButtonStyle" TargetType="Button" BasedOn="{StaticResource AppIconSelectedButton}"/>
        <Style x:Name="SelectedButtonStyle" TargetType="Button" BasedOn="{StaticResource AppSelectedButtonStyle}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
        </Style>

        <!--Flyout-->
        <Flyout x:Name="MoreFlyout" Placement="Right" FlyoutPresenterStyle="{StaticResource AppFlyoutPresenterStyle}">
            <StackPanel x:Name="MoreStackPanel">
                <!--MoreChildren-->
            </StackPanel>
        </Flyout>
        
    </UserControl.Resources>


    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden" HorizontalAlignment="Stretch">
        <StackPanel x:Name="StackPanel" Padding="{StaticResource AppPanelPadding}" HorizontalAlignment="Stretch">
            <!--Children-->
        </StackPanel>
    </ScrollViewer >

</UserControl>