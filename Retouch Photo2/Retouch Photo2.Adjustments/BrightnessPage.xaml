<Page
    x:Class="Retouch_Photo2.Adjustments.Pages.BrightnessPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hsvColorPickers="using:HSVColorPickers"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="200"
    >

    <Page.Resources>

        <!--White to Light-->
        <LinearGradientBrush x:Name="WhiteLightBrush" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="White" Offset="0"/>
            <GradientStop Color="DimGray" Offset="1"/>
        </LinearGradientBrush>
        
        <!--White to Dark-->
        <LinearGradientBrush x:Name="WhiteDarkBrush" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="DimGray" Offset="0"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>

        <!--Black to Light-->
        <LinearGradientBrush x:Name="BlackLightBrush" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="Gainsboro" Offset="1"/>
        </LinearGradientBrush>

        <!--Black to Dark-->
        <LinearGradientBrush x:Name="BlackDarkBrush" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="Gainsboro" Offset="0"/>
            <GradientStop Color="Black" Offset="1"/>
        </LinearGradientBrush>
        
    </Page.Resources>
    

    <StackPanel >

        <!--White to Light-->
        <TextBlock x:Name="WhiteToLightTextBlock" Style="{StaticResource AppTextBlockStyle}"/>
        <hsvColorPickers:TouchSlider x:Name="WhiteLightSlider"/>

        <!--White to Dark-->
        <TextBlock x:Name="WhiteToDarkTextBlock" Style="{StaticResource AppTextBlockStyle}"/>
        <hsvColorPickers:TouchSlider x:Name="WhiteDarkSlider"/>

        <!--Black to Light-->
        <TextBlock x:Name="BlackToLightTextBlock" Style="{StaticResource AppTextBlockStyle}"/>
        <hsvColorPickers:TouchSlider x:Name="BlackLightSlider"/>

        <!--Black to Dark-->
        <TextBlock x:Name="BlackToDarkTextBlock" Style="{StaticResource AppTextBlockStyle}"/>
        <hsvColorPickers:TouchSlider x:Name="BlackDarkSlider"/>

    </StackPanel>
</Page>