<UserControl
    x:Class="Retouch_Photo.Controls.EffectsControls.EffectControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="200">
    
        <Grid Height="50" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource AppBorderBrush}">

        <Button  x:Name="Button"
                 HorizontalAlignment="Stretch"
                 HorizontalContentAlignment="Left"
                 Content="{x:Bind Effect.Icon}"
                 Loaded="{x:Bind Effect.Button_Loaded}"
                 
                 Width="Auto"
                 Style="{ThemeResource NavigationBackButtonNormalStyle}"
                 
                 Tapped="Button_Tapped"/>

        <Border Width="50" HorizontalAlignment="Right">
            <ToggleSwitch Loaded="{x:Bind Effect.ToggleSwitch_Loaded}" Toggled="ToggleSwitch_Toggled">
                    <ToggleSwitch.Clip>
                        <RectangleGeometry Rect="0,0,50,50"/>
                    </ToggleSwitch.Clip>
                </ToggleSwitch>
            </Border>
            
        </Grid>

</UserControl>
