<UserControl
    x:Class="Retouch_Photo.Controls.AppbarControls.DuplicateControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo.Controls.AppbarControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conAppbarControls="using:Retouch_Photo.Controls.AppbarControls"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="800">


    <StackPanel x:Name="StackPanel" MinWidth="260">

        <!--VisualState-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>

                <!--Phone-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Vertical"/>
                    </VisualState.Setters>
                </VisualState>

                <!--Pad-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Horizontal"/>
                    </VisualState.Setters>
                </VisualState>

                <!--PC-->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="900"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanel.Orientation" Value="Horizontal"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <StackPanel Margin="8" VerticalAlignment="Center">
            <TextBlock Text="Dupliate Projects" FontWeight="Bold"/>
            <TextBlock Text="Select project(s) to be duplicated." FontSize="12" Style="{ThemeResource AppTextBlock}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <AppBarSeparator/>
            <conAppbarControls:AppBarControlButton x:Name="OKButton" Glyph="&#xE10B;"  Text="OK" Tap="OKButton_Tapped"/>
            <conAppbarControls:AppBarControlButton x:Name="CancelButton" Glyph="&#xE106;"  Text="Cancel" Tap="CancelButton_Tapped"/>
        </StackPanel>

    </StackPanel>

</UserControl>
