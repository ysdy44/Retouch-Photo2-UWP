<UserControl
    x:Class="Retouch_Photo.Controls.MainCanvasControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:lib="using:Retouch_Photo.Library"
    xmlns:blendcontrol="using:Retouch_Photo.Controls.BlendControl"
    mc:Ignorable="d"
    Drop="UserControl_Drop"
    d:DesignHeight="800"
    d:DesignWidth="800">

    <UserControl.Resources>

        <!--Pointer-->
        <lib:CanvasOperator  x:Key="CanvasOperator"
   
            DestinationControl="{Binding ElementName=CanvasControl}"
            
            Single_Start="Single_Start" 
            Single_Delta="Single_Delta"
            Single_Complete="Single_Complete"
            
            Right_Start="Right_Start"
            Right_Delta="Right_Delta"
            Right_Complete="Right_Complete"
            
            Double_Start="Double_Start"
            Double_Delta="Double_Delta"
            Double_Complete="Double_Complete"
            
            Wheel_Changed="Wheel_Changed"
            
            />
        
    </UserControl.Resources>

    <canvas:CanvasControl x:Name="CanvasControl" UseSharedDevice="True" CustomDevice="{x:Bind ViewModel.CanvasDevice}"/>

</UserControl>
