<UserControl
    x:Class="Retouch_Photo.Controls.LayerControls.LayerAdjustmentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo.Controls.LayerControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Retouch_Photo.Models"
    mc:Ignorable="d"
    Visibility="Collapsed"
    d:DesignHeight="50"
    d:DesignWidth="200">

    <StackPanel  Width="200" >

        <TextBlock Text="Adjustment" FontWeight="Medium" Margin="0,6,0,4" Foreground="{ThemeResource SystemControlBackgroundBaseMediumBrush}" />
      
        <Border Style="{StaticResource AppRoundedBorderStyle}">
            <ItemsControl x:Name="AdjustmentsItemsControl" >
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:Adjustment">
                        <Grid>
                            <ContentPresenter Content="{x:Bind Icon}"/>
                            <Button Height="40" 
                                    HorizontalAlignment="Right" 
                                    HorizontalContentAlignment="Left"
                                    Style="{ThemeResource DateTimePickerFlyoutButtonStyle}" 
                                    Tapped="{x:Bind RemoveButton_Tapped}">
                                <FontIcon FontSize="12" Glyph="&#xE106;" FontFamily="Segoe MDL2 Assets"/>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
        
    </StackPanel>
   
    
</UserControl>
