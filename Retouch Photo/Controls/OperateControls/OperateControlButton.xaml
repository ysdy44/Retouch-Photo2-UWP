<UserControl
    x:Class="Retouch_Photo.Controls.OperateControls.OperateControlButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo.Controls.OperateControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <SolidColorBrush x:Name="EnabledColor" Color="{ThemeResource SystemBaseHighColor}"/>
        <SolidColorBrush x:Name="DisenabledColor" Color="Gray"/>
    </UserControl.Resources>

    <Button x:Name="Button" Height="40" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Style="{ThemeResource DateTimePickerFlyoutButtonStyle}" Tapped="Button_Tapped">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Viewbox x:Name="EnabledViewbox" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Viewbox x:Name="DisabledViewbox" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock x:Name="TextBlock" Grid.Column="1" FontSize="14"  VerticalAlignment="Center"/>
        </Grid>
    </Button>
</UserControl>
