<UserControl
    x:Class="Retouch_Photo.Pickers.ColorPicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo.Pickers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="270"
    d:DesignWidth="200">


    <UserControl.Resources>

        <!--Color-->
        <SolidColorBrush x:Name="SolidColorBrushName" x:Key="SolidColorBrushKey" Color="White"/>

    </UserControl.Resources>


    <StackPanel >

        <!--Head-->
        <Grid Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <Ellipse x:Name="Ellipse" Width="22" Height="22" Fill="{StaticResource SolidColorBrushKey}" />
                <Ellipse StrokeThickness="4" Width="25" Height="25" Stroke="Gray"/>
                <Ellipse StrokeThickness="2" Width="23" Height="23" Stroke="White"/>
            </Grid>
            <TextBlock Visibility="Collapsed" Grid.Column="1" Text="Color" FontWeight="Medium" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <local:StrawPicker x:Name="StrawPicker" Grid.Column="2"/>
        </Grid>

        <!--Toggle-->
        <Grid Grid.Row="1" Height="38" CornerRadius="8" BorderThickness="0,0,1,1"  BorderBrush="{ThemeResource AppStroke}"  Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
            <ComboBox x:Name="ComboBox"  ItemsSource="{x:Bind Pickers}"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      BorderThickness="0" BorderBrush="Transparent" Background="Transparent"
                      >
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="local:Picker">
                        <TextBlock Text="{x:Bind Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <!--Body-->
        <ContentControl x:Name="ContentControl" Grid.Row="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />

        <!--Alpha-->
        <local:AlphaPicker x:Name="AlphaPicker" />

        <ContentControl Height="12"/>

    </StackPanel>
</UserControl>
