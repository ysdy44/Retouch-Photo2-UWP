<Page
    x:Class="Retouch_Photo.Pages.EffectPages.OuterShadowPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo.Pages.EffectPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pic="using:Retouch_Photo.Pickers"
    xmlns:element="using:Retouch_Photo.Element"
    mc:Ignorable="d"
    >

    <Page.Resources>
        <Flyout x:Name="ColorFlyout" FlyoutPresenterStyle="{StaticResource AppFlyoutPresenterStyle}">
            <pic:ColorPicker x:Name="ColorPicker" Width="200" ColorChange="ColorPicker_ColorChange"/>
        </Flyout>
    </Page.Resources>

    <StackPanel >
        <TextBlock Text="Radius" Style="{StaticResource AppTextBlock}"/>
        <Slider x:Name="RadiusSlider" Minimum="0" Maximum="100" ValueChanged="RadiusSlider_ValueChanged" Style="{ThemeResource AppOpacitySliderStyle}"/>

        <TextBlock Text="Opacity" Style="{StaticResource AppTextBlock}"/>
        <Slider x:Name="OpacitySlider" Minimum="0" Maximum="100" ValueChanged="OpacitySlider_ValueChanged" Style="{ThemeResource AppOpacitySliderStyle}"/>

        <TextBlock Text="Offset" Style="{StaticResource AppTextBlock}"/>
        <Slider x:Name="OffsetSlider" Minimum="0" Maximum="100" ValueChanged="OffsetSlider_ValueChanged" Style="{ThemeResource AppOpacitySliderStyle}"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*" MaxWidth="100"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" Text="Radians" Style="{StaticResource AppTextBlock}"/>
            <element:RadiansPicker x:Name="RadiansPicker" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top"
                               BorderBrush="{ThemeResource AppBorderBrush}"
                               Background="{ThemeResource AppBackground}"
                               RadiansChange="RadiansPicker_RadiansChange"/>

            <TextBlock Grid.Column="1" Grid.Row="0" Text="Color" Style="{StaticResource AppTextBlock}"/>
            <Border x:Name="ColorButton" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Style="{StaticResource AppCircleBorderStyle}" Tapped="ColorButton_Tapped">
                <Border.Background>
                    <SolidColorBrush  x:Name="SolidColorBrush"/>
                </Border.Background>
            </Border>
            
        </Grid>
        
    </StackPanel>
</Page>