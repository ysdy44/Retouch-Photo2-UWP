<UserControl
    x:Class="Retouch_Photo.Pages.PageControls.NumberControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo.Pages.PageControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="100">

    <Border MinWidth="100" Style="{StaticResource AppRoundedBorderStyle}">
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1"/>
                <ColumnDefinition Width="28"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TextBox" Text="12 %" TextWrapping="NoWrap" Width="Auto" BorderBrush="Transparent" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" TextChanged="TextBox_TextChanged"/>
            <Border Grid.Column="1" Background="{ThemeResource AppBorderBrush}"/>
            <Button Grid.Column="2"  Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"  BorderThickness="0" Background="Transparent">
                <FontIcon FontSize="12" Glyph="&#xE935;" FontFamily="Segoe MDL2 Assets"/>
                <Button.Flyout>
                    <Flyout FlyoutPresenterStyle="{StaticResource AppFlyoutPresenterStyle}">
                        <Slider x:Name="Slider" Width="400" TickFrequency="25" TickPlacement="Inline" Style="{StaticResource AppOpacitySliderStyle}" ValueChanged="Slider_ValueChanged"/>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </Grid>
    </Border>
</UserControl>
