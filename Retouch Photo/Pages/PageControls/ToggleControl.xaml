<UserControl
    x:Class="Retouch_Photo.Pages.PageControls.ToggleControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Retouch_Photo.Pages.PageControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="250">


    <UserControl.Resources>

        <!--Background-->
        <SolidColorBrush x:Name="AccentColor"  Color="{ThemeResource SystemAccentColor}"/>
        <SolidColorBrush x:Name="UnAccentColor" Color="#00000000"/>

        <!--Foreground-->
        <SolidColorBrush x:Name="CheckColor" Color="White"/>
        <SolidColorBrush x:Name="UnCheckColor" Color="{ThemeResource SystemBaseHighColor}"/>

    </UserControl.Resources>

    <Border Style="{StaticResource AppRoundedBorderStyle}">
        <ContentPresenter x:Name="ContentPresenter" MinWidth="60" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Tapped="ContentPresenter_Tapped"/>
    </Border>

</UserControl>
