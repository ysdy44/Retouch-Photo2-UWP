<tool:ToolPage
    x:Class="Retouch_Photo.Tools.Pages.RectanglePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:element="using:Retouch_Photo.Element"
    xmlns:pic="using:Retouch_Photo.Pickers"
    xmlns:tool="using:Retouch_Photo.Tools"
    mc:Ignorable="d">

    <tool:ToolPage.Resources>
        <Flyout x:Name="ColorFlyout" FlyoutPresenterStyle="{StaticResource AppFlyoutPresenterStyle}">
            <pic:ColorPicker x:Name="ColorPicker" Width="200"/>
        </Flyout>
    </tool:ToolPage.Resources>

    <ScrollViewer HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Hidden" VerticalScrollMode="Disabled" VerticalScrollBarVisibility="Disabled">
        <StackPanel Orientation="Horizontal" Height="50" Padding="4">

            <!--Mode-->
            <element:ModeControl Mode="{x:Bind ViewModel.MarqueeMode,Mode=TwoWay}" />

            
            <AppBarSeparator/>
            <Border x:Name="ColorButton" Style="{StaticResource AppCircleBorderStyle}">
                <Border.Background>
                    <SolidColorBrush Color="{x:Bind ViewModel.Color,Mode=OneWay}"/>
                </Border.Background>
            </Border>
            
            
        </StackPanel>
    </ScrollViewer>

</tool:ToolPage>
